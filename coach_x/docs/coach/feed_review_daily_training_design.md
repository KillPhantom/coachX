# Training Feed Page - Design Document

**Feature**: 沉浸式训练批阅流（Immersive Training Review Feed）
**Version**: 1.0
**Date**: 2025-11-17
**Status**: Design Phase

---

## 一、设计理念（Design Philosophy）

### 1.1 核心概念

**从"信息陈列页"到"待批阅任务队列"**

传统的训练审核页面是一个信息陈列页，教练需要在复杂的界面中导航、点击、滚动，才能完成批阅任务。新设计将页面重新定义为一个**"待批阅任务队列"**，借鉴 TikTok/Reels 的交互模式，将教练的思维流（"看-判断-反馈"）与工具的交互流（"刷-上滑-输入"）完全统一。

### 1.2 设计目标

1. **沉浸式体验**：全屏内容，最小化干扰，教练专注于训练内容本身
2. **流畅的批阅节奏**：90% 的内容只需上滑跳过，10% 需要留反馈
3. **零摩擦反馈**：输入框始终可见，点击即可展开评论区
4. **情境不丢失**：评论时视频缩小保留在顶部，教练可边看边写

### 1.3 用户体验流

```
教练进入页面
  ↓
[批阅状态] 全屏视频/图文卡片，上下滑动切换
  ├─ 无问题 → 上滑跳过（自动标记已批阅）
  ├─ 需要反馈 → 点击输入框
  │    ↓
  │  [反馈状态] 视频缩小，评论区滑出
  │    ├─ 输入反馈 → 发送 → 关闭评论区
  │    └─ 取消 → 关闭评论区
  │
  └─ 查看详情 → 点击进度徽章
       ↓
     [详情状态] Bottom Sheet 显示训练总览
       └─ 关闭 → 返回批阅状态
```

---

## 二、架构设计（Architecture）

### 2.1 核心概念模型

#### Feed Item（Feed 流单元）

Feed Item 是批阅流的基本单元，每个 Item 代表一个需要批阅的内容。

**类型**：
- **视频项**：学生上传的训练视频（每个视频独立一个 Feed Item）
- **图文项**：无视频的 Exercise（汇总显示组数、重量、次数等）
- **完成项**：占位符，表示所有内容已批阅完成

**生成规则**：
```
DailyTraining
├─ Exercise 1 (3 个视频)
│  ├─ Video 1 → Feed Item 1 (视频项)
│  ├─ Video 2 → Feed Item 2 (视频项)
│  └─ Video 3 → Feed Item 3 (视频项)
├─ Exercise 2 (0 个视频)
│  └─ [Summary] → Feed Item 4 (图文项)
├─ Exercise 3 (1 个视频)
│  └─ Video 1 → Feed Item 5 (视频项)
└─ [Completion] → Feed Item 6 (完成项)
```

### 2.2 三层状态机（State Machine）

```
┌─────────────────────────────────────────┐
│  状态一：批阅状态 (Feed State)           │
│  ┌───────────────────────────────────┐  │
│  │   全屏视频/图文卡片                │  │
│  │   顶部：导航栏 + 进度徽章          │  │
│  │   底部：反馈输入框                 │  │
│  └───────────────────────────────────┘  │
│  手势：上滑 → 下一个 Feed Item          │
│  点击：输入框 → 状态二                  │
│  点击：进度徽章 → 状态三                │
└─────────────────────────────────────────┘
              ↓                    ↓
┌──────────────────────┐  ┌──────────────────────┐
│ 状态二：反馈状态      │  │ 状态三：详情状态      │
│ ┌──────────────────┐ │  │ ┌──────────────────┐ │
│ │ 视频缩小 (30vh)   │ │  │ │ Bottom Sheet     │ │
│ └──────────────────┘ │  │ │ - 概览卡片       │ │
│ ┌──────────────────┐ │  │ │ - 动作列表       │ │
│ │ 评论区 (70vh)     │ │  │ │ - 营养数据       │ │
│ │ - 历史反馈       │ │  │ └──────────────────┘ │
│ │ - 输入区         │ │  │                      │
│ └──────────────────┘ │  │                      │
└──────────────────────┘  └──────────────────────┘
```

### 2.3 数据流（Data Flow）

```
┌─────────────────────────────────────────────────────────┐
│  Firestore: dailyTrainings/{id}                         │
│  - exercises[]: [{exerciseId, name, sets[], videos[]}]  │
│  - extractedKeyFrames: {...}                            │
│  - isReviewed: false                                    │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  Repository: TrainingFeedRepository                     │
│  - generateFeedItems() → List<TrainingFeedItem>         │
│  - markFeedItemReviewed(feedItemId)                     │
│  - updateVideoReviewStatus(exerciseId, videoIndex)      │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  Provider: feedItemsProvider                            │
│  - 监听 dailyTraining 变化                              │
│  - 自动重新生成 Feed Items                              │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  UI: TrainingFeedPage (PageView)                        │
│  - 渲染 Feed Items                                      │
│  - 处理手势（上滑 → 标记 isReviewed）                   │
│  - 管理状态（批阅/反馈/详情）                            │
└─────────────────────────────────────────────────────────┘
```

---

## 三、数据结构（Data Models）

### 3.1 TrainingFeedItem（核心数据模型）

```dart
class TrainingFeedItem {
  final String id;                    // 唯一标识：{dailyTrainingId}_{type}_{index}
  final FeedItemType type;            // video | textCard | completion
  final String dailyTrainingId;       // 关联的训练记录 ID
  final String? exerciseId;           // Exercise ID（video/textCard 有值）
  final String? exerciseName;         // Exercise 名称
  final bool isReviewed;              // 是否已批阅
  final Map<String, dynamic>? metadata; // 额外数据
}

enum FeedItemType {
  video,       // 视频项
  textCard,    // 图文项
  completion,  // 完成占位符
}
```

**metadata 结构**：

**视频项**：
```json
{
  "videoUrl": "https://...",
  "thumbnailUrl": "https://...",
  "videoIndex": 0,
  "duration": 120
}
```

**图文项**：
```json
{
  "sets": [
    {"reps": 8, "weight": 100, "completed": true},
    {"reps": 8, "weight": 100, "completed": true}
  ],
  "totalSets": 5,
  "completedSets": 5,
  "avgWeight": 100,
  "totalReps": 40
}
```

### 3.2 VideoModel（标准化视频模型）

```dart
class VideoModel {
  final String videoUrl;
  final String? thumbnailUrl;
  final bool isReviewed;    // NEW
  final int? duration;
}
```

**存储位置**：`dailyTrainings/{id}/exercises[i]/videos[j]`

### 3.3 TrainingFeedbackModel（重构后）

```dart
class TrainingFeedbackModel {
  final String id;
  final String dailyTrainingId;    // 必填
  final String studentId;
  final String coachId;
  final String trainingDate;
  final String? exerciseId;        // NEW: 可选（视频项填，图文项不填）
  final String? exerciseName;
  final String feedbackType;       // "text" | "voice" | "image"
  final String? textContent;
  final String? voiceUrl;
  final int? voiceDuration;
  final String? imageUrl;
  final int createdAt;
  final bool isRead;
}
```

**关键变更**：
- ❌ 移除：`planId`, `exerciseIndex`
- ✅ 新增：`exerciseId`（可选，用于关联 Exercise）

**查询场景**：

1. **视频项历史反馈**：
   ```dart
   feedbackRepo.getExerciseHistoryFeedbacks(
     studentId: 'xxx',
     exerciseId: 'ex_uuid_123',
   );
   // 查询条件：studentId + exerciseId
   ```

2. **图文项历史反馈**：
   ```dart
   feedbackRepo.getDailyTrainingFeedbacks(
     dailyTrainingId: 'dt_xxx',
   );
   // 查询条件：dailyTrainingId + exerciseId == null
   ```

---

## 四、交互流程（Interaction Flow）

### 4.1 批阅状态（Feed State）

#### 视频项

**布局**：
```
┌─────────────────────────────────────┐
│ [< 返回] 学生名 · 今日训练 [批阅: 3/8]│ ← 顶部叠层
├─────────────────────────────────────┤
│ 杠铃卧推                        [▶] │ ← 动作信息卡片
├─────────────────────────────────────┤
│                                     │
│                                     │
│         [全屏视频播放]               │
│                                     │
│                                     │
├─────────────────────────────────────┤
│  ━━━━|──|─────|────── 00:15 / 1:23  │ ← 关键帧时间轴
│     0:05 0:12  0:18                 │    （时间刻度 + 缩略图）
│      ↓    ↓     ↓                   │
│     [🖼️] [🖼️]  [🖼️]                 │
│                                     │
│                          ┌───┐      │ ← Floating Button
│                          │📸 │      │    （仅暂停时显示）
│                          │截取││      │
│                          │关键帧││      │
│                          └───┘      │
├─────────────────────────────────────┤
│ [点评 "杠铃卧推"...]         [→]    │ ← 底部输入框
└─────────────────────────────────────┘
```

**交互**：
1. **自动播放**：进入 Feed Item 时自动播放视频
2. **上滑**：标记 `video.isReviewed = true`，进入下一个 Feed Item
3. **点击输入框**：进入反馈状态
4. **点击进度徽章**：进入详情状态
5. **点击关键帧缩略图**：
   - 视频跳转到该时间点（暂停）
   - 自动打开 `ImagePreviewPage`
6. **点击 Floating Button**：
   - 打开 `ImagePreviewPage`（关键帧管理）

#### 图文项

**布局**：
```
┌─────────────────────────────────────┐
│ [< 返回] 学生名 · 今日训练 [批阅: 5/8]│
├─────────────────────────────────────┤
│                                     │
│         深蹲                         │
│     ─────────────────────           │
│     完成组数: 5/5                    │
│     平均重量: 100kg                  │
│     总次数: 40 次                    │
│                                     │
│     [渐变背景]                       │
│                                     │
├─────────────────────────────────────┤
│ [点评 "深蹲"...]             [→]    │
└─────────────────────────────────────┘
```

**交互**：
1. **上滑**：标记 `exercise.isReviewed = true`，进入下一个 Feed Item
2. **点击输入框**：进入反馈状态（反馈不绑定 exerciseId）

#### 完成项

**布局**：
```
┌─────────────────────────────────────┐
│ [< 返回] 学生名 · 今日训练 [批阅: 8/8]│
├─────────────────────────────────────┤
│                                     │
│              🎉                      │
│                                     │
│        已全部批阅完成！               │
│                                     │
│      点击返回查看更多学生             │
│                                     │
└─────────────────────────────────────┘
```

**交互**：
1. **点击任意位置**：返回 Coach Home Page
2. **自动触发**：`dailyTraining.isReviewed = true`

---

### 4.2 反馈状态（Comment State）

#### 布局

**视频缩小 + 评论区滑出**：
```
┌─────────────────────────────────────┐
│ [缩小的视频 30vh]                    │ ← 可点击关闭评论区
├─────────────────────────────────────┤
│ 点评 "杠铃卧推"              [×]    │ ← 头部（取消按钮）
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │ ← 下拉手柄
│                                     │
│   历史反馈                           │
│   ┌───────────────────────────┐    │
│   │ [教] Coach Ivan           │    │
│   │ 2023-11-15                │    │
│   │ 动作整体不错！注意下背部...│    │
│   └───────────────────────────┘    │
│   ┌───────────────────────────┐    │
│   │ [教] Coach Ivan           │    │
│   │ 2023-11-12                │    │
│   │ 今天的下降速度控制得很好...│    │
│   └───────────────────────────┘    │
│                                     │
│   [加载更多]                         │
├─────────────────────────────────────┤
│   输入反馈区                         │
│   ┌─────────────────────────────┐  │
│   │ [多行文本框]                 │  │
│   │                             │  │
│   └─────────────────────────────┘  │
│                        [发送]       │
└─────────────────────────────────────┘
```

#### 交互逻辑

**打开**：
1. 点击底部输入框
2. 主视频容器缩小到 30vh（动画 300ms）
3. 评论区从底部滑出 70vh（动画 400ms）
4. 加载历史反馈（基于 `exerciseId` 或 `dailyTrainingId`）

**关闭**（3 种方式）：
1. **头部下拉手柄向下滑动**：
   ```dart
   GestureDetector(
     onVerticalDragEnd: (details) {
       if (details.primaryVelocity > 300) {
         closeCommentSheet();
       }
     },
   )
   ```

2. **点击外部区域**（顶部视频缩略图）：
   ```dart
   GestureDetector(
     onTap: () => closeCommentSheet(),
     child: Container(height: 30vh, child: VideoThumbnail(...)),
   )
   ```

3. **点击取消按钮 [×]**：
   ```dart
   CupertinoButton(
     child: Icon(CupertinoIcons.xmark),
     onPressed: () => closeCommentSheet(),
   )
   ```

**发送反馈**：
1. 用户输入文本
2. 点击"发送"按钮
3. 调用 `feedbackRepo.addFeedback(...)`
4. 反馈成功后，**不自动关闭**评论区（用户手动下滑或点击外部关闭）
5. 历史反馈区自动刷新（Firestore Stream）

---

### 4.3 详情状态（Detail State）

#### 布局

**Bottom Sheet（85vh）**：
```
┌─────────────────────────────────────┐
│ 今日训练总览                    [×] │ ← 头部
├─────────────────────────────────────┤
│   概览卡片                           │
│   ┌──────────┬──────────┐          │
│   │ 完成概况  │ 总组数    │          │
│   │ 4/5      │ 21       │          │
│   │ 80%      │ 组       │          │
│   └──────────┴──────────┘          │
│                                     │
│   动作列表                           │
│   ┌───────────────────────────┐    │
│   │ 杠铃卧推                   │    │
│   │ 5/5 组完成                 │    │
│   └───────────────────────────┘    │
│   ┌───────────────────────────┐    │
│   │ 上斜卧推                   │    │
│   │ 4/4 组完成                 │    │
│   └───────────────────────────┘    │
│   ┌───────────────────────────┐    │
│   │ 深蹲                       │    │
│   │ 4/5 组完成                 │    │
│   └───────────────────────────┘    │
│                                     │
│   营养数据                           │
│   [复用 DietPlanCard]               │
│                                     │
└─────────────────────────────────────┘
```

#### 数据计算

**完成概况**：
```dart
// 完全完成的 Exercise 数量
final completedCount = exercises.where((e) =>
  e.sets.every((s) => s.completed)
).length;

// 总 Exercise 数量
final totalCount = exercises.length;

// 完成百分比
final percentage = (completedCount / totalCount * 100).toInt();
```

**总组数**：
```dart
final totalSets = exercises.fold<int>(
  0,
  (sum, e) => sum + e.sets.length
);
```

---

## 五、视频播放与关键帧系统（Video & Keyframe System）

### 5.1 视频播放器（FeedVideoPlayer）

#### 特性

1. **自动播放**：
   - 进入 Feed Item 时自动播放
   - 使用 `CachedVideoPlayerPlus` + `VideoCacheService`（1 小时缓存）

2. **可拖动进度条**：
   - 用户可拖动滑块跳转到任意位置
   - 实时显示当前播放时间 / 总时长

3. **播放/暂停控制**：
   - 点击视频区域 → 暂停/播放
   - 暂停时显示 Floating Button（截取关键帧）

4. **预加载优化**：
   - 预下载当前 ± 1 个 Feed Item 的视频
   - 后台预下载（用于关键帧提取）

#### 生命周期

```dart
class FeedVideoPlayer extends StatefulWidget {
  @override
  _FeedVideoPlayerState createState() => _FeedVideoPlayerState();
}

class _FeedVideoPlayerState extends State<FeedVideoPlayer> {
  CachedVideoPlayerPlusController? _controller;
  bool _isPaused = false;
  String? _downloadedVideoPath;  // 预下载路径

  @override
  void initState() {
    super.initState();
    _initializeVideo();
    _predownloadVideo();  // 后台预下载
  }

  @override
  void dispose() {
    _controller?.dispose();
    _cleanupDownloadedVideo();
    super.dispose();
  }
}
```

---

### 5.2 关键帧时间轴（KeyframeTimeline）

#### 设计规格

**样式**：时间刻度 + Floating 缩略图

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ← 进度条（白色）
    |      |        |           ← 时间刻度线（垂直短线）
   0:05   0:12     0:18         ← 时间文本
    ↓      ↓        ↓
   [🖼️]   [🖼️]     [🖼️]         ← Floating 缩略图（悬停放大）
```

**组件结构**：
```dart
Stack(
  children: [
    // 进度条
    Container(height: 3, color: Colors.white.withOpacity(0.3)),

    // 关键帧标记（循环）
    for (var keyframe in keyframes)
      Positioned(
        left: _calculatePosition(keyframe.timestamp, videoDuration),
        child: Column(
          children: [
            // 时间刻度线
            Container(width: 2, height: 10, color: Colors.white),

            // 时间文本
            Text(_formatTime(keyframe.timestamp)),

            // 缩略图
            GestureDetector(
              onTap: () => onKeyframeTap(keyframe.timestamp),
              child: Container(
                width: 60, height: 60,
                child: CachedNetworkImage(url: keyframe.url),
              ),
            ),
          ],
        ),
      ),
  ],
)
```

#### 位置计算

```dart
double _calculatePosition(double timestamp, Duration videoDuration) {
  final screenWidth = MediaQuery.of(context).size.width - 40;  // 减去左右 padding
  final progress = timestamp / videoDuration.inSeconds;
  return screenWidth * progress;
}
```

#### 交互逻辑

```dart
void onKeyframeTap(double timestamp) {
  // 1. 暂停视频
  videoController.pause();

  // 2. 跳转到该时间点
  videoController.seekTo(Duration(seconds: timestamp.toInt()));

  // 3. 打开 ImagePreviewPage
  Navigator.push(
    context,
    MaterialPageRoute(
      builder: (_) => ImagePreviewPage(
        dailyTrainingId: dailyTrainingId,
        exerciseIndex: exerciseIndex,
        keyframes: allKeyframes,
        initialTimestamp: timestamp,  // 高亮当前关键帧
        allowEditing: true,
      ),
    ),
  );
}
```

---

### 5.3 关键帧浮动按钮（KeyframeFloatingButton）

#### 显示条件

```dart
// 仅在视频暂停时显示
if (!videoController.value.isPlaying)
  KeyframeFloatingButton(
    onTap: () => _openKeyframeManager(),
  )
```

#### 样式规格

```dart
Container(
  width: 60,
  height: 60,
  decoration: BoxDecoration(
    shape: BoxShape.circle,
    color: Colors.black.withOpacity(0.6),
    boxShadow: [
      BoxShadow(
        color: Colors.black.withOpacity(0.3),
        blurRadius: 8,
        offset: Offset(0, 4),
      ),
    ],
  ),
  child: Column(
    mainAxisAlignment: MainAxisAlignment.center,
    children: [
      Icon(CupertinoIcons.camera, size: 24, color: Colors.white),
      SizedBox(height: 2),
      Text(
        '截取\n关键帧',
        style: AppTextStyles.caption2.copyWith(
          color: Colors.white,
          height: 1.1,
        ),
        textAlign: TextAlign.center,
      ),
    ],
  ),
)
```

#### 点击行为

```dart
void _openKeyframeManager() {
  Navigator.push(
    context,
    MaterialPageRoute(
      builder: (_) => ImagePreviewPage(
        dailyTrainingId: dailyTrainingId,
        exerciseIndex: exerciseIndex,
        keyframes: existingKeyframes,
        allowEditing: true,
      ),
    ),
  );
}
```

---

### 5.4 关键帧管理页面（ImagePreviewPage）

#### 功能扩展

**新增参数**：
```dart
class ImagePreviewPage extends StatefulWidget {
  final String dailyTrainingId;
  final int exerciseIndex;
  final List<KeyframeModel> keyframes;      // NEW: 现有关键帧列表
  final bool allowEditing;                  // NEW: 是否允许编辑
  final double? initialTimestamp;           // NEW: 初始高亮的时间戳
}
```

**布局**：
```
┌─────────────────────────────────────┐
│ [关闭] 关键帧管理           [添加]   │ ← 头部
├─────────────────────────────────────┤
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐           │
│  │🖼️ │ │🖼️ │ │🖼️ │ │🖼️ │           │ ← 网格列表（3 列）
│  │0:05│ │0:12│ │0:18│ │0:25│           │
│  └───┘ └───┘ └───┘ └───┘           │
│                                     │
│  ┌───┐ ┌───┐                        │
│  │🖼️ │ │🖼️ │                        │
│  │0:32│ │0:45│                        │
│  └───┘ └───┘                        │
│                                     │
└─────────────────────────────────────┘
```

**交互**：
1. **点击缩略图**：放大查看
2. **长按缩略图**：显示操作菜单（编辑 / 删除）
3. **点击"添加"按钮**：
   - 返回视频播放器
   - 用户调整时间
   - 点击截取按钮
   - 关键帧添加到列表

---

### 5.5 自动提取逻辑（禁用）

**决策**：完全禁用 `LocalKeyframeExtractor`

**原因**：
1. 手动提取更精准（教练选择关键时刻）
2. 减少后台计算负担
3. 避免存储大量无用关键帧

**保留方案**：
- 代码保留（注释掉）
- 文档标记为"未来 AI 功能"
- Firestore 数据结构保留（支持 `method: "auto"` 字段）

**未来启用路径**：
```dart
// 在 VideoPlayerDialog 或 FeedVideoPlayer 中
// Future.delayed(Duration(seconds: 2), () {
//   if (widget.enableAutoExtraction) {
//     _autoExtractKeyframes();
//   }
// });
```

---

## 六、UI/UX 规范（UI/UX Guidelines）

### 6.1 颜色系统

**主色调**：
- 背景：`#000000`（纯黑，强化视频沉浸感）
- 文本：`#FFFFFF`（纯白，高对比度）
- 叠层背景：`rgba(0, 0, 0, 0.6)`（半透明黑）
- 主题色：`#f2e8cf`（暖米色，用于按钮/徽章）

**状态色**：
- 已批阅：`#4CAF50`（绿色）
- 待批阅：`#FFC107`（黄色）
- 无视频：`#9E9E9E`（灰色）

### 6.2 字体规范

**严格遵守 AppTextStyles**：

| 场景 | 样式 | 大小 | 权重 |
|------|------|------|------|
| 导航栏标题 | `navTitle` | 17px | SemiBold (600) |
| 动作名称 | `title3` | 20px | SemiBold (600) |
| 进度徽章 | `subhead` | 15px | Regular (400) |
| 输入框提示 | `callout` | 16px | Regular (400) |
| 时间戳 | `caption1` | 12px | Regular (400) |
| Floating Button 文字 | `caption2` | 11px | Regular (400) |

### 6.3 动画规范

**页面切换**：
- PageView 滑动：`duration: 300ms, curve: Curves.easeInOut`

**状态切换**：
- 视频缩小（批阅 → 反馈）：`duration: 300ms, curve: Curves.easeInOut`
- 评论区滑出：`duration: 400ms, curve: Curves.easeOutCubic`
- Bottom Sheet 滑出：`duration: 400ms, curve: Curves.easeOutCubic`

**微交互**：
- 按钮按下：`Transform.scale(0.95), duration: 100ms`
- 关键帧缩略图悬停：`Transform.scale(1.1), duration: 200ms`

### 6.4 手势规范

**PageView 滑动**：
- 上滑：下一个 Feed Item（阈值：50px）
- 下滑：上一个 Feed Item（仅第 2+ 个 Feed Item）

**评论区下拉**：
- 下滑速度：> 300 px/s → 关闭评论区
- 触发区域：仅头部区域（下拉手柄）

**视频播放器**：
- 点击：播放/暂停
- 拖动进度条：跳转到指定位置

---

## 七、性能优化（Performance Optimization）

### 7.1 视频缓存策略

**复用现有 VideoCacheService**：
- 缓存有效期：1 小时
- 存储路径：`/tmp/training_videos/`
- 清理时机：App 启动时清理过期缓存

**预加载策略**：
```dart
// 预加载当前 ± 1 个 Feed Item 的视频
void _preloadVideos(int currentIndex) {
  final feedItems = ref.read(feedItemsProvider);

  // 预加载上一个
  if (currentIndex > 0) {
    _preloadVideo(feedItems[currentIndex - 1]);
  }

  // 预加载下一个
  if (currentIndex < feedItems.length - 1) {
    _preloadVideo(feedItems[currentIndex + 1]);
  }
}
```

### 7.2 图片优化

**关键帧缩略图**：
- 使用 `CachedNetworkImage`
- 缩略图尺寸：60x60px（Keyframe Timeline）
- 压缩质量：70%
- 缓存策略：内存 + 磁盘双缓存

**图文卡片背景**：
- 使用 CSS 渐变（不加载图片）
- 减少内存占用

### 7.3 Lazy Loading

**PageView 懒加载**：
```dart
PageView.builder(
  itemCount: feedItems.length,
  itemBuilder: (context, index) {
    // 仅初始化当前 ± 1 的视频播放器
    if ((index - currentIndex).abs() <= 1) {
      return _buildFeedItem(feedItems[index]);
    } else {
      return Container();  // 占位符
    }
  },
)
```

### 7.4 内存管理

**视频播放器资源释放**：
```dart
@override
void dispose() {
  // 释放播放器
  _controller?.dispose();

  // 清理预下载的视频文件
  if (_downloadedVideoPath != null) {
    File(_downloadedVideoPath!).deleteSync();
  }

  super.dispose();
}
```

---

## 八、测试策略（Testing Strategy）

### 8.1 单元测试

**数据模型测试**：
- `TrainingFeedItem` 生成逻辑
- Feed Items 排序规则
- `isReviewed` 状态更新

**Repository 测试**：
- `generateFeedItems` 方法
- `markFeedItemReviewed` 方法
- Firestore 查询逻辑

### 8.2 Widget 测试

**组件测试**：
- `VideoFeedItem` 自动播放
- `KeyframeTimeline` 位置计算
- `FeedCommentSheet` 滑动关闭
- `KeyframeFloatingButton` 显示/隐藏

### 8.3 集成测试

**完整流程测试**：
1. 进入页面 → Feed Items 加载正确
2. 上滑 → 标记 `isReviewed` → 进度徽章更新
3. 点击输入框 → 评论区打开 → 留反馈 → 发送成功
4. 点击关键帧缩略图 → 视频跳转 → 打开预览页
5. 点击 Floating Button → 打开关键帧管理
6. 所有 Feed 刷完 → 显示完成页 → 返回 Coach Home

### 8.4 性能测试

**关键指标**：
- Feed 切换延迟：< 100ms
- 视频加载时间：< 2s（10s 视频）
- 关键帧提取时间：< 500ms
- 评论区打开动画：400ms（流畅）

---

## 九、未来增强（Future Enhancements）

### 9.1 AI 辅助功能

1. **自动关键帧提取**（已预留）：
   - 启用 `LocalKeyframeExtractor`
   - 基于动作幅度自动识别关键时刻
   - 教练可审核和删除无用关键帧

2. **AI 反馈建议**：
   - 基于历史反馈，AI 提示常见问题
   - 例如："上次提到需要注意下背部，本次是否改善？"

3. **动作分析**：
   - 集成姿态估计（Pose Estimation）
   - 实时标注关节角度、运动轨迹

### 9.2 批量操作

**批量批阅模式**：
- 教练可选择"快速批阅"模式
- 自动跳过已批阅的 Feed Items
- 仅显示待批阅内容

### 9.3 多教练协作

**分配批阅任务**：
- 主教练可分配学生给助理教练
- 助理教练完成批阅后，主教练审核

### 9.4 学生视角

**查看教练反馈**：
- 学生可在 Training Feed 中查看教练反馈
- 点击关键帧查看教练标注的问题点

---

## 十、技术债务与风险（Technical Debt & Risks）

### 10.1 已知限制

1. **视频文件大小**：
   - 单个视频 > 100MB 时，预下载耗时较长
   - 缓解方案：提示学生压缩视频或限制时长

2. **关键帧数量**：
   - 单个 Exercise 超过 20 个关键帧时，Timeline 拥挤
   - 缓解方案：支持横向滚动或分页显示

3. **网络依赖**：
   - 弱网环境下，视频加载缓慢
   - 缓解方案：显示加载进度条，支持离线缓存

### 10.2 潜在风险

1. **数据一致性**：
   - 如果教练在批阅过程中，学生更新了训练记录
   - 缓解方案：Firestore Stream 实时同步，显示提示"数据已更新，请刷新"

2. **误操作**：
   - 教练误上滑导致未留反馈就标记已批阅
   - 缓解方案：支持"撤销"操作（1 分钟内可撤销）

3. **存储成本**：
   - 大量关键帧图片占用 Firebase Storage 空间
   - 缓解方案：定期清理旧关键帧（> 30 天）

---

## 十一、Firestore 数据结构示例（Firestore Schema Example）

### dailyTrainings 集合

```json
{
  "id": "dt_20231115_student123",
  "studentID": "student_123",
  "coachID": "coach_456",
  "date": "2023-11-15",
  "isReviewed": false,
  "exercises": [
    {
      "exerciseId": "ex_uuid_001",
      "name": "杠铃卧推",
      "sets": [
        {"reps": 8, "weight": 100, "completed": true, "rpe": 7},
        {"reps": 8, "weight": 100, "completed": true, "rpe": 7.5}
      ],
      "videos": [
        {
          "videoUrl": "https://storage.googleapis.com/...",
          "thumbnailUrl": "https://storage.googleapis.com/...",
          "isReviewed": false,
          "duration": 120
        },
        {
          "videoUrl": "https://storage.googleapis.com/...",
          "thumbnailUrl": "https://storage.googleapis.com/...",
          "isReviewed": true,
          "duration": 115
        }
      ]
    },
    {
      "exerciseId": "ex_uuid_002",
      "name": "深蹲",
      "sets": [
        {"reps": 5, "weight": 120, "completed": true, "rpe": 8},
        {"reps": 5, "weight": 120, "completed": true, "rpe": 8.5}
      ],
      "videos": []
    }
  ],
  "extractedKeyFrames": {
    "0": {
      "exerciseName": "杠铃卧推",
      "keyframes": [
        {
          "localPath": "/tmp/manual_keyframes/frame_001.jpg",
          "url": "https://storage.googleapis.com/...",
          "timestamp": 5.2,
          "uploadStatus": "uploaded"
        },
        {
          "localPath": "/tmp/manual_keyframes/frame_002.jpg",
          "url": "https://storage.googleapis.com/...",
          "timestamp": 12.8,
          "uploadStatus": "uploaded"
        }
      ],
      "extractedAt": "2023-11-15T10:30:00Z",
      "method": "manual"
    }
  },
  "diet": {
    "totalCalories": 2500,
    "protein": 180,
    "carbs": 300,
    "fat": 80
  },
  "createdAt": 1700041200000,
  "updatedAt": 1700041500000
}
```

### dailyTrainingFeedback 集合

```json
{
  "id": "feedback_001",
  "dailyTrainingId": "dt_20231115_student123",
  "studentId": "student_123",
  "coachId": "coach_456",
  "trainingDate": "2023-11-15",
  "exerciseId": "ex_uuid_001",
  "exerciseName": "杠铃卧推",
  "feedbackType": "text",
  "textContent": "动作整体不错！注意下背部要保持自然弧度。",
  "voiceUrl": null,
  "voiceDuration": null,
  "imageUrl": null,
  "createdAt": 1700041800000,
  "isRead": false
}
```

---

## 十二、关键决策记录（Key Decision Log）

| 决策项 | 方案 | 原因 | 日期 |
|--------|------|------|------|
| Feed Item 粒度 | 每个视频独立一个 Feed Item | 教练需要逐个审核每个视频 | 2025-11-17 |
| 无视频 Exercise | 生成图文 Feed Item（汇总式） | 保持批阅流完整性，避免遗漏 | 2025-11-17 |
| 反馈关联 | 视频项绑定 `exerciseId`，图文项仅绑定 `dailyTrainingId` | 区分 Exercise 级别和训练级别的反馈 | 2025-11-17 |
| 自动关键帧提取 | 完全禁用（代码保留） | 手动提取更精准，减少后台负担 | 2025-11-17 |
| 关键帧时间轴 | 时间刻度 + Floating 缩略图 | 直观显示关键时刻，支持快速定位 | 2025-11-17 |
| 评论区关闭方式 | 3 种（下拉、点击外部、取消按钮） | 提供多种退出路径，符合用户习惯 | 2025-11-17 |
| 数据模型调整 | 移除 `planId`/`exerciseIndex`，新增 `exerciseId` | 全局唯一 ID 更灵活，支持跨计划查询 | 2025-11-17 |
| 历史数据兼容 | 不考虑（collection 已清空） | 无历史数据负担，直接使用新模型 | 2025-11-17 |

---

## 十三、参考文档（References）

1. **现有实现**：
   - [Daily Training Review Page](./daily_training_review_page_implementation.md)
   - [Manual Keyframe Extraction Feature](./manual_keyframe_extraction_feature.md)

2. **后端 API**：
   - [Backend APIs and Database Schemas](../backend_apis_and_document_db_schemas.md)

3. **设计系统**：
   - [App Text Styles](../../lib/core/theme/app_text_styles.dart)
   - [App Colors](../../lib/core/theme/app_colors.dart)

4. **项目规范**：
   - [CLAUDE.md](../../CLAUDE.md)

---

**Document Version**: 1.0
**Last Updated**: 2025-11-17
**Author**: Claude Code
**Review Status**: Design Complete, Ready for Implementation Planning
