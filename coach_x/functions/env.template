# Firebase Functions 环境变量配置模板
# 
# 本文件用于本地开发环境配置参考
# 请将此文件复制为 .env 并填入实际值
# 
# 注意：
# - .env 文件包含敏感信息，已被 .gitignore 忽略
# - 生产环境使用 Firebase Functions Secrets 管理
# 
# 使用方法：
# 1. 复制此文件：cp env.template .env
# 2. 编辑 .env 并填入实际值
# 3. 启动 Emulator：
#    export $(cat .env | xargs) && firebase emulators:start --only functions

# ==================== AI 服务配置 ====================

# Anthropic Claude API Key
# 获取方式：https://console.anthropic.com/
# 生产环境使用：firebase functions:secrets:set ANTHROPIC_API_KEY
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Claude 模型名称（可选）
# 默认：claude-sonnet-4-20250514
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# 最大 Token 数量（可选）
# 默认：4096
ANTHROPIC_MAX_TOKENS=4096

# Temperature 参数（可选）
# 默认：0.7
ANTHROPIC_TEMPERATURE=0.7

# ==================== 其他配置 ====================

# 日志级别（可选）
# 选项：DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

